{
  "name": "hexcloud",
  "private": true,
  "workspaces": ["apps/web", "backend"],
  "scripts": {
    "dev": "concurrently -n api,next,legacy -c cyan,magenta,yellow \"npm run dev --prefix backend\" \"npm run dev --prefix apps/web\" \"npm run dev --prefix frontend\"",
    "dev:api": "npm run dev --prefix backend",
    "dev:next": "npm run dev --prefix apps/web",
    "dev:web": "npm run dev --prefix frontend",
    "build": "npm run build --prefix backend && npm run build --prefix apps/web",
    "db:migrate": "npm run prisma:migrate --prefix backend",
    "docker:up": "docker compose -f docker/docker-compose.yml up -d"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
